{
  "name": "@aportaya/simulado",
  "version": "0.0.0",
  "private": true,
  "description": "Servidor simulado derivado de los contratos OpenAPI. Lo consumen los tres productos.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "contratos": "node scripts/contratos-a-json.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src scripts",
    "//": "Sin corredor propio: lo que este paquete produce se verifica donde se consume, en apps/movil/pruebas/{unidad,contrato}."
  },
  "dependencies": {
    "msw": "^2.15.0",
    "randexp": "^0.5.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.39.0",
    "eslint-config-expo": "^10.0.0",
    "typescript": "~5.9.2",
    "yaml": "^2.9.0"
  }
}
