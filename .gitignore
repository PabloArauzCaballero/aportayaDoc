# macOS
.DS_Store

# Obsidian: estado local del editor, no del modelo
docs/.obsidian/workspace.json
docs/.obsidian/workspace-mobile.json
docs/.obsidian/cache

# Python
__pycache__/
*.pyc

# Editor / IDE: estado local, no del proyecto
.vscode/
.claude/launch.json

# Andamio vacío: apps/ se quitó del repo (commit 63ec074) y se reintroduce
# cuando exista código de verdad, no como carpeta con solo un .gitignore
apps/

# --- generado por el build: el gate es la compilacion, no un diff (ADR-016) ---
build/
.gradle/
servicios/*/build/
plataforma/*/build/
clientes/typescript/
despliegue/k8s/generado/

# --- secretos: nunca versionados (docs/Seguridad.md §3.4, prohibición 1) ---
.env
.env.*
!.env.ejemplo
*.pem
*.key
*.p12
*.jks
