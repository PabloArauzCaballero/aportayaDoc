package bo.aportaya.nucleofinanciero.dominio;

/** {@code ck_asiento_contable_origen_tipo} — no hay un décimo valor fuera de la base. */
public enum OrigenAsiento {
    PAGO,
    ENTREGA,
    COBERTURA,
    COMISION,
    BILLETERA,
    AJUSTE,
    COBRO_CXC,
    DEPRECIACION_ACTIVO,
    FACTURA_PROVEEDOR
}
